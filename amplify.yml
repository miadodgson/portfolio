frontend:
  phases:
    build:
      commands:
        - yarn install
        - yarn build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**

# # Rewrite rules
# redirects:
#   - source: "/<*>.glb"
#     target: "/<*>.glb"
#     status: 200
#   - source: "/<*>"
#     target: "/index.html"
#     status: 200
